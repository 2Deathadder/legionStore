import { Link } from 'react-router-dom'
import { ArrowUpRight, ShoppingBag } from 'lucide-react'
import { useStore } from '../context/StoreContext'
export default function ProductCard({ product }) { const { addToCart } = useStore(); const unavailable = product.stock < 1; return <article className="group overflow-hidden border border-zinc-800 bg-zinc-900/70"><Link to={`/produit/${product.id}`}><div className="relative aspect-[4/3] overflow-hidden bg-zinc-800"><img src={product.image} alt={product.name} className="h-full w-full object-cover opacity-80 transition duration-500 group-hover:scale-105 group-hover:opacity-100"/><span className="absolute left-4 top-4 border border-zinc-600 bg-zinc-950/80 px-2 py-1 text-[10px] uppercase tracking-widest text-zinc-300">{product.featured ? 'Sélection' : 'Legion'}</span></div></Link><div className="p-5"><div className="mb-3 flex items-start justify-between gap-3"><div><h2 className="font-semibold">{product.name}</h2><p className="mt-1 text-xs text-zinc-500">{product.specs.Graphique}</p></div><ArrowUpRight size={18} className="text-zinc-500"/></div><div className="flex items-center justify-between"><strong className="text-lg">{product.price.toLocaleString('fr-FR')} €</strong><button disabled={unavailable} onClick={() => addToCart(product)} className="flex items-center gap-2 bg-[#ef3340] px-3 py-2 text-xs font-bold text-white transition hover:bg-red-500 disabled:cursor-not-allowed disabled:bg-zinc-700"><ShoppingBag size={15}/> {unavailable ? 'Indisponible' : 'Ajouter'}</button></div></div></article> }