import { Link, NavLink } from 'react-router-dom'
import { ShoppingBag, Zap } from 'lucide-react'
import { useStore } from '../context/StoreContext'
export default function Header() { const { cart } = useStore(); const count = cart.reduce((n, i) => n + i.quantity, 0); return <header className="sticky top-0 z-20 border-b border-zinc-800 bg-zinc-950/95 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4"><Link to="/" className="flex items-center gap-2 font-bold tracking-tight"><span className="grid h-8 w-8 place-items-center bg-[#ef3340] text-zinc-950"><Zap size={18} fill="currentColor"/></span><span>LEGION<span className="text-[#ef3340]">STORE</span></span></Link><nav className="hidden gap-7 text-sm text-zinc-400 md:flex"><NavLink to="/" className={({isActive}) => isActive ? 'text-white' : 'hover:text-white'}>Catalogue</NavLink><NavLink to="/contact" className="hover:text-white">Contact</NavLink><NavLink to="/admin" className="hover:text-white">Administration</NavLink></nav><Link to="/panier" aria-label="Panier" className="relative flex items-center gap-2 text-sm hover:text-[#ef3340]"><ShoppingBag size={20}/><span className="hidden sm:inline">Panier</span>{count > 0 && <b className="absolute -right-3 -top-3 grid h-5 min-w-5 place-items-center rounded-full bg-[#ef3340] px-1 text-[10px] text-white">{count}</b>}</Link></div></header> }