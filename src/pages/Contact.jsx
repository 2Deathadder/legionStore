import { useState } from 'react'
import { useStore } from '../context/StoreContext'
export default function Contact() { const { submitMessage } = useStore(); const [done, setDone] = useState(false); const [form, setForm] = useState({name:'', email:'', message:''}); const update = e => setForm({...form, [e.target.name]: e.target.value}); const submit = async e => { e.preventDefault(); await submitMessage(form); setDone(true) }; return <section className="mx-auto grid max-w-7xl gap-16 px-5 py-16 lg:grid-cols-[.7fr_1fr]"><div><p className="text-xs uppercase tracking-[.2em] text-[#ef3340]">Support Legion</p><h1 className="mt-4 text-5xl font-bold leading-tight">Une question avant de passer à l'action ?</h1><p className="mt-6 leading-7 text-zinc-400">Écrivez-nous pour une question sur un modèle, sa disponibilité ou votre demande de commande.</p></div>{done ? <div className="border border-[#72e06a] p-8"><h2 className="text-2xl font-bold text-[#72e06a]">Message envoyé</h2><p className="mt-3 text-zinc-400">Nous avons bien reçu votre message.</p></div> : <form onSubmit={submit} className="border border-zinc-800 bg-zinc-900 p-6 sm:p-8"><label className="block text-sm text-zinc-400">Nom<input required name="name" value={form.name} onChange={update} className="mt-2 w-full border border-zinc-700 bg-zinc-950 px-3 py-3 text-white"/></label><label className="mt-5 block text-sm text-zinc-400">Email<input required type="email" name="email" value={form.email} onChange={update} className="mt-2 w-full border border-zinc-700 bg-zinc-950 px-3 py-3 text-white"/></label><label className="mt-5 block text-sm text-zinc-400">Message<textarea required name="message" value={form.message} onChange={update} rows="6" className="mt-2 w-full border border-zinc-700 bg-zinc-950 px-3 py-3 text-white"/></label><button className="mt-6 bg-[#ef3340] px-6 py-3 text-sm font-bold text-white">Envoyer le message</button></form>}</section> }